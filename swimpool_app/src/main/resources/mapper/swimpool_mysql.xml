<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spacrud.swimpool_app.swimpool.service.SwimpoolMapper">
    <select id="findAll" resultType="SwimpoolDTO">
        select id, name, lanes, lanes_type, phone_number, addr
        from swimpool_tbl
        order by id desc
    </select>
    <insert id="insert" parameterType="SwimpoolDTO" useGeneratedKeys="true" keyProperty="id">
        insert into swimpool_tbl(name, lanes, lanes_type, phone_number, addr)
        values(#{name}, #{lanes}, #{lanesType}, #{phoneNumber}, #{addr})
    </insert>
    <delete id="delete" parameterType="Integer">
        delete from swimpool_tbl where id = #{id}
    </delete>
    <update id="update" parameterType="SwimpoolDTO">
        update swimpool_tbl set name = #{name},
                                lanes = #{lanes},
                                lanes_type = #{lanesType},
                                phone_number = #{phoneNumber},
                                addr = #{addr}
        where id = #{id}
    </update>

</mapper>